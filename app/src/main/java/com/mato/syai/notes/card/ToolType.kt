package com.mato.syai.notes.card

enum class ToolType {
    None,           // No tool selected (default)
    Text,           // Text tool (T)
    Shape,          // Shape tool
    Drawing,        // Freehand brush
    ColorPicker,    // Color selection tool
    Bullet,         // Bullet points & to-do checklists
    Layer,          // Layer reorder tool
    AiAssistant     // AI Assistant operations
}
